{
  "name": "docs-playground-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && npm start",
    "build": "cd frontend && npm install && npm run build && node -e \"require('fs').cpSync('frontend/dist', 'backend/dist', {recursive: true})\" && cd ../backend && npm install"
  }
}
